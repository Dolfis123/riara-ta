server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # PENTING: Agar saat refresh halaman tidak 404
        try_files $uri $uri/ /index.html;
    }
}